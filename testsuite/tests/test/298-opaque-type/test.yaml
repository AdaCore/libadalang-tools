description: |
  Check that TGen supports types that are derived from an opaque type.

default-gpr: true

driver: gnattest_tgen

control:
    - [XFAIL, 'x86', 'Marshalling not working for 32bits (UB03-008)']
