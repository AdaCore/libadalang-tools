description: Test that generating test case vectors for private subprograms
  without a private formal works. Also check that the tool rejects private
  subprograms with a private parameter type.

driver: shell_script

control:
    - [XFAIL, 'x86', 'Marshalling not working for 32bits (UB03-008)']
