package Main_Package is
   procedure Foo
     (A, B, C : Integer;
      D, E, F : in Integer;
      G, H, I : out Integer;
      J, K, L : in out Integer);

   procedure Qux
     (M : Integer; N, O, P : Integer);
end Main_Package;
