
Info: AdaCore provides a new formatter GNATformat, currently in beta. This will supersede GNATpp when leaving the beta program.
-------

with Ada.Text_IO; use Ada.Text_IO;
procedure Main is
   Corge_Garply : Integer  := 1;
   Foo_Bar      : constant := 1;
begin
   Put_Line (Foo_Bar'Image);
end Main;


Info: AdaCore provides a new formatter GNATformat, currently in beta. This will supersede GNATpp when leaving the beta program.
-------

with Ada.Text_IO; use Ada.Text_IO;
procedure Main is
   Corge_Garply : Integer  := 1;
   Foo_Bar      : constant := 1;
begin
   Put_Line (Foo_Bar'Image);
end Main;


Info: AdaCore provides a new formatter GNATformat, currently in beta. This will supersede GNATpp when leaving the beta program.
-------

with Ada.Text_IO; use Ada.Text_IO;
procedure Main is
   Corge_Garply : Integer  := 1;
   foo_Bar      : constant := 1;
begin
   Put_Line (foo_Bar'Image);
end Main;


Info: AdaCore provides a new formatter GNATformat, currently in beta. This will supersede GNATpp when leaving the beta program.
-------

with Ada.Text_IO; use Ada.Text_IO;
procedure Main is
   Corge_Garply : Integer  := 1;
   FOO_BAR      : constant := 1;
begin
   Put_Line (FOO_BAR'Image);
end Main;


Info: AdaCore provides a new formatter GNATformat, currently in beta. This will supersede GNATpp when leaving the beta program.
-------

with Ada.Text_IO; use Ada.Text_IO;
procedure Main is
   Corge_Garply : Integer  := 1;
   foo_bar      : constant := 1;
begin
   Put_Line (foo_bar'Image);
end Main;


Info: AdaCore provides a new formatter GNATformat, currently in beta. This will supersede GNATpp when leaving the beta program.
-------

with Ada.Text_IO; use Ada.Text_IO;
procedure Main is
   Corge_Garply : Integer  := 1;
   Foo_Bar      : constant := 1;
begin
   Put_Line (Foo_Bar'Image);
end Main;
