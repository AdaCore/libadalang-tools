description: |
    This test checks if the safe rename tool correctly handles
    the rename of a enum literal. Only collisions with other
    enum literal sibblings are considered.

driver: shell_script
